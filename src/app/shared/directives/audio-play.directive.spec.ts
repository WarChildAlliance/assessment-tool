import { AudioPlayDirective } from './audio-play.directive';

describe('AudioPlayDirective', () => {
  it('should create an instance', () => {
    const directive = new AudioPlayDirective();
    expect(directive).toBeTruthy();
  });
});
